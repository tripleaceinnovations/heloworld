name: myCreate comment on new issues

on:
  issues:
    types: [opened]
    
jobs:
  comment-wih-action:
    runs-on: ubuntu-latest
    steps:
      - name: "dump github context"
        run" echo '${{ toJSON(github.event) }}' | jq
        shell: bash
